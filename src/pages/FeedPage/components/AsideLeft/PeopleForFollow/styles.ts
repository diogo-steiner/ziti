import styled from "styled-components";

export const PeopleForFollowContainer = styled.section`
  padding: 1.5rem;
  background-color: var(--c-white);
  box-shadow: var(--shadow);
  border-radius: var(--b-radius-1);
`;

export const ListPeople = styled.ul`
  margin-top: 1.5rem;
  display: flex;
  flex-direction: column;
  gap: 1rem;
`;
