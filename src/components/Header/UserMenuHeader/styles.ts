import styled from "styled-components";

export const UserMenuHeaderContainer = styled.div`
  position: relative;

  .button__arrow {
    position: absolute;
    right: -0.125rem;
    bottom: -0.25rem;
    background-color: var(--c-gray-600);
    width: 1rem;
    height: 1rem;
    border-radius: 50%;
    text-align: center;
  }
`;
